# Docker Environment Variables - Production
# Copy this file to .env.docker and fill in your values

# MongoDB Configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change-this-secure-password
MONGODB_URI=mongodb://admin:change-this-secure-password@mongodb:27017/smart-todo-app?authSource=admin

# JWT Configuration (MUST be at least 32 characters)
JWT_SECRET=change-this-to-a-strong-secret-at-least-32-characters-long

# Frontend/Backend URLs
CLIENT_URL=http://localhost
VITE_API_URL=http://localhost:5000/api

# OpenAI API Key (optional)
OPENAI_API_KEY=

# ==============================================
# Production Deployment Notes:
# ==============================================
# 1. Generate strong passwords for MongoDB
# 2. Use a cryptographically secure JWT_SECRET
#    Example: openssl rand -base64 32
# 3. Update CLIENT_URL to your production domain
# 4. Update VITE_API_URL to your production API URL
# 5. NEVER commit .env.docker with real credentials
# ==============================================
